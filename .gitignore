# Transient artifacts from the /task skill — copy/paste into SCRUM + GitHub
task.md
pr.md

# Datasets — download manually, never commit
*.csv
*.zip
*Zone.Identifier

# Local env overrides — copy from .env.example
.env

# IDE
.idea/
